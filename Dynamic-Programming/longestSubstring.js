var longestSubstring=function(s){
    const strLen = s.length;
    if(strLen==0) return "";
    let start= 0;
    let maxLength =1;
    const dpArray =Array.from({length: n},()=>Array(n).fill(false));
    
    
     

}